'use strict';

// FUNCTIONS //


// PDF //

/**
* FUNCTION: pdf( x, p )
*	Evaluates the probability density function (PDF) for a Geometric distribution with success probability `p` at a value `x`.
*
* @param {Number} x - input value
* @param {Number} p - success probability
* @returns {Number} evaluated PDF
*/
function pdf( x, p ) {

} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;
